# ezlb 日志增强与流量可观测性方案

## 背景
- 当前配置中仅存在 `global.log_level` 与 `global.cleanup_on_exit`，尚未形成统一的 `global.log` 配置块。
- 现有日志能力以单一系统日志为主，缺少面向流量与 NAT 统计的独立日志输出能力。
- 需要以**极低开销**获取业务流量可见性，优先复用 IPVS / SNAT 现有统计信息，避免在转发热路径中引入逐请求埋点。
- 系统日志（System）、流量日志（Traffic）、NAT 日志（NAT）应具备清晰职责边界：
  - **System Logger**：用于启动、配置变更、reconcile、健康检查、异常等系统行为；
  - **Traffic Logger**：用于周期性输出 IPVS 流量增量统计；
  - **NAT Logger**：用于 SNAT 规则变更与 NAT 统计。
- 交付物包含：代码改动、本文档更新、README/README_CN 更新、示例配置更新、覆盖新能力的单测与 e2e / integration 测试。

## 目标
1. 建立统一的日志配置模型与 logger 构建方式，保证各模块日志字段一致、便于排障与采集。
2. 增加低开销流量日志能力，支持 **service 粒度的日志级别控制**。
3. 引入 NAT 统计日志，但在第一阶段明确按**规则/后端维度**输出；当多个 service 共享 backend 时，**不保证 service 级精确归因**。
4. 完成配置、文档、测试与运行行为定义，确保 macOS/Linux 开发环境与 Linux 运行环境均可验证。

## 非目标（Out of Scope）
本次方案**不包含**以下能力：

1. 请求级实时 access log；
2. 每连接级审计日志；
3. 在数据转发热路径中增加逐请求埋点；
4. 多 service 共享同一 backend 时的 NAT 精确 service 归因；
5. 完整替代 Prometheus / metrics 系统的长期监控体系。

## 配置升级说明

本次改造属于**日志配置模型升级**，不是在现有 `global.log_level` 上追加少量字段。

### 现状
当前 `pkg/config/config.go` 中仅包含：
- `global.log_level`
- `global.cleanup_on_exit`

### 目标结构
新增并统一使用（`traffic` 配置内聚到 `global.log` 下，保持日志配置单一入口）：

```yaml
global:
  log:
    level: info
    home: ./logs
    max_size: 50
    max_backups: 3
    max_age: 0
    compress: false
    traffic:
      enabled: true
      interval: 15s
      include_snat: true
```

> **设计决策**：`traffic` 本质是日志子系统的一部分，收纳到 `global.log` 下而非作为平级配置块，避免用户在两个不同层级查找日志相关配置。

### 迁移策略
- **不需要兼容旧字段**，直接使用新结构，在 README / 发布说明。
- `global.log.home` 如果未配置或者配置为空，使用默认值`./logs`。

## 总体实施策略

建议分两阶段实施，降低改造风险。

### Phase 1：日志基础设施改造
- 新增 `global.log` 配置块；
- 统一 logger 初始化；
- 引入 system / traffic / nat 三套 logger；
- 接入日志文件滚动；
- 更新 README / 示例配置 / 单测；
- 暂不引入完整 traffic collector。

### Phase 2：低开销流量与 NAT 可观测性
- 实现 IPVS 流量增量采集；
- 实现可选 SNAT 统计采集；
- 接入 traffic logger 周期输出；
- 接入 config 热更新；
- 补齐 Linux integration/e2e 测试。

## 实施计划

### 1. 日志基础设施改造

#### 1.1 配置结构升级（`pkg/config/config.go`）
- `GlobalConfig` 中新增 `LogConfig`：
  - `level`
  - `home`
  - `max_size`
  - `max_backups`
  - `max_age`
  - `compress`
- `LogConfig` 内嵌 `TrafficLogConfig`：
  - `enabled`
  - `interval`
  - `include_snat`
- `ServiceConfig` 新增：
  - `traffic_log`（`*bool` 类型，默认 nil 即不记录）
- 默认值建议：
  - `global.log.level = info`
  - `global.log.home = ./logs`
  - `global.log.max_size = 50`
  - `global.log.max_backups = 3`
  - `global.log.max_age = 0`
  - `global.log.compress = false`
  - `global.log.traffic.enabled = true`
  - `global.log.traffic.interval = 15s`
  - `global.log.traffic.include_snat = true`
- 校验规则：
  - `global.log.level` 仅允许 `debug/info/warn/error`
  - `service.traffic_log` 为 `bool` 类型（`true` 开启，`false` 或不设置则关闭）
  - `global.log.traffic.interval` 最小值建议为 `5s`
- **新依赖**：需新增 `gopkg.in/natefinch/lumberjack.v2` 用于日志文件滚动（当前 `go.mod` 中不存在）。

#### 1.2 Logger 初始化（`cmd/ezlb/main.go` + 新增 `pkg/logutil`）

**职责划分**：
- `pkg/logutil` 负责提供 **logger 构建工具函数**（如 `NewSystemLogger`、`NewTrafficLogger`、`NewNATLogger`），封装 `zapcore.NewTee` + `lumberjack` 的组合逻辑。
- `cmd/ezlb/main.go`（或 `pkg/server`）负责 **logger 的生命周期管理**（创建、注入、Sync），保持现有的依赖注入风格。

**初始化时序（必须明确）**：
当前代码中 `config.NewManager()` 本身需要 logger 参数，因此 system logger 必须在配置加载之前就能创建。推荐的初始化流程：
1. `main.go` 中先用**默认配置**（info level + stdout）创建一个 bootstrap system logger；
2. 使用 bootstrap logger 初始化 `config.Manager` 并加载配置；
3. 读取 `global.log` 配置后，调用 `pkg/logutil` 构建正式的三套 logger（含文件输出、滚动等）；
4. 将正式 logger 注入到 server 及各模块；bootstrap logger 完成使命后 Sync 并丢弃。

**已有 bug 修复**：当前 `cmd/ezlb/main.go` 中的 `newLogger()` 硬编码 `zap.InfoLevel`，从未读取 `global.log_level`（现为 `global.log.level`）配置。本次改造需一并修复，确保 logger 级别与配置一致。

- 设计 `buildLoggers(cfg LogConfig)`，统一创建三套 logger：
  - **System Logger**
  - **Traffic Logger**
  - **NAT Logger**
- 使用 `zapcore.NewTee` + `lumberjack` 构建多输出 core。
- 文件名本期固定为：
  - `ezlb.log`
  - `traffic.log`
  - `nat.log`
- 输出规则：
   - System Logger → stdout/stderr + `${home}/ezlb.log`
   - Traffic Logger → `${home}/traffic.log`
   - NAT Logger → `${home}/nat.log`
- 文件打开 / 目录创建失败时的策略：
  - 优先记录告警；
  - 回退到 stdout/stderr；
  - **不建议**因 traffic/nat 日志文件创建失败直接阻断服务启动。

> **注意**：当前 CLI 中不存在 `--log-level` 参数（日志级别一直通过配置文件控制），无需删除。

#### 1.3 统一字段辅助方法（`pkg/logutil/fields.go`）
- 提供：
  - `ServiceFields(serviceCfg)`
  - `BackendFields(serviceCfg, backendCfg)`
- 建议统一字段：
  - `service`
  - `listen`
  - `protocol`
  - `scheduler`
  - `backend`
  - `full_nat`
  - `snat_ip`
- 目标：减少各模块手写字段，保证日志结构稳定。

#### 1.4 模块接入范围
- 核心模块继续使用 root/system logger：
  - config
  - server
  - reconciler
  - healthcheck
  - lvs manager
- Traffic collector 使用 traffic logger。
- SNAT 规则变更与 NAT 统计使用 NAT logger。

#### 1.5 测试
- 单测覆盖：
  - `LogConfig` 默认值与校验
  - `TrafficLogConfig` 默认值与校验
  - `log.home` 默认值行为
  - logger core 组合与 fallback 行为
  - 字段辅助方法输出

### 2. 低开销流量日志

#### 2.1 配置扩展（`pkg/config/config.go`）
- `ServiceConfig.traffic_log` 为 `*bool` 类型，**默认为 nil（即不记录）**。
- 只有在 service 层面明确设置 `traffic_log: true` 时，才会记录该 service 的流量日志。
- 所有流量日志统一以 **debug 级别**输出原始累计数据。
- `global.log.traffic.enabled=false` 时，不启动 traffic collector。

#### 2.2 统计数据模型与接口约束

在编码前，需先定义 stats provider 接口与数据模型，避免实现阶段各模块各自推断。

建议引入：

```go
type ServiceTrafficStats struct {
    ServiceKey        string
    Connections       uint64
    InPkts            uint64
    OutPkts           uint64
    InBytes           uint64
    OutBytes          uint64
}

type BackendTrafficStats struct {
    ServiceKey        string
    BackendKey        string
    Connections       uint64
    InPkts            uint64
    OutPkts           uint64
    InBytes           uint64
    OutBytes          uint64
}

type SNATRuleStats struct {
    RuleKey           string
    BackendKey        string
    Packets           uint64
    Bytes             uint64
}

type TrafficSnapshot struct {
    Services map[string]ServiceTrafficStats
    Backends map[string]BackendTrafficStats
    SNAT     map[string]SNATRuleStats
}
```

接口建议：

```go
type LVSStatsProvider interface {
    ServiceStats() (map[string]ServiceTrafficStats, error)
    BackendStats() (map[string]BackendTrafficStats, error)
}

type SNATStatsProvider interface {
    Stats() (map[string]SNATRuleStats, error)
}
```

**LVSStatsProvider 实现策略**：
现有 `IPVSHandle.GetServices()` 和 `IPVSHandle.GetDestinations()` 已经返回了 `SvcStats` / `DstStats`（`ipvs_handle_linux.go` 中 `fromMobyService` / `fromMobyDestination` 已填充统计字段）。因此：
- **推荐**：基于 `lvs.Manager` 实现一个适配器（adapter），直接复用 `GetServices()` / `GetDestinations()` 获取统计数据，**不修改 `IPVSHandle` 接口**（避免 fake 实现同步改动）。
- 示例：
  ```go
  // pkg/trafficlog/lvs_stats_adapter.go
  type lvsStatsAdapter struct {
      manager *lvs.Manager
  }
  func (a *lvsStatsAdapter) ServiceStats() (map[string]ServiceTrafficStats, error) { ... }
  func (a *lvsStatsAdapter) BackendStats() (map[string]BackendTrafficStats, error) { ... }
  ```

**类型精度说明**：现有 `SvcStats.Connections` 为 `uint32`（来自 moby/ipvs），而本方案中 `ServiceTrafficStats.Connections` 定义为 `uint64`。实现时需注意 `uint32 → uint64` 的转换，`uint64` 范围更大，适合增量计算场景。

**Fake 环境行为（macOS 开发）**：
- `fakeHandle` 当前不产生真实统计数据（CRUD 操作不更新 Stats 字段），因此 fake 环境下 `LVSStatsProvider` 返回的统计值全为 0。
- Traffic collector 在 fake 环境下正常运行，但因默认所有 service 的 `traffic_log` 为 nil（禁用），不会输出任何 traffic 日志。
- 即使某个 service 显式开启了 traffic log，fake 环境下统计值全为 0，日志仍会输出（记录原始数据，值为 0 也是有效数据点）。
- 这保证了 macOS 开发环境下 collector 逻辑可被测试（启动/停止/配置变更）。

**数据记录策略**：
- **记录原始累计数据**（raw stats），而非增量 delta。每次采集周期直接输出 IPVS 返回的累计统计值。
- 不再维护 `prevSnapshot`，不再计算增量。
- 每次采集周期都会输出日志（只要 service 开启了 traffic log），无论数据是否变化。
- 所有 traffic / NAT 日志统一使用 **debug 级别**输出。
- Traffic / NAT logger 的文件 level 固定为 debug，确保 debug 日志能写入文件。文件轮转使用全局统一的轮转规则（`max_size`、`max_backups`、`max_age`、`compress`）。

#### 2.3 统计收集组件（`pkg/trafficlog/`）

> **包路径决策**：使用 `pkg/trafficlog/` 而非 `pkg/telemetry/trafficlogger/`，与现有的 `pkg/snat/`、`pkg/lvs/` 等扁平结构保持一致。当前项目中没有其他 telemetry 子模块，无需提前引入 `pkg/telemetry/` 层级。
- 依赖 `LVSStatsProvider` 与可选 `SNATStatsProvider`。
- 每次采集周期直接输出原始累计数据（raw stats），不维护快照、不计算增量。
- 按 service 配置决定是否输出：
  - `traffic_log` 为 nil 或 `false` → 不输出（**默认行为**）；
  - `traffic_log: true` → 输出，统一使用 debug 级别。
- 每次采集周期都输出（只要 service 开启了 traffic log），无论数据是否变化。
- 若 stats 获取失败：
  - 记录 system logger warn/error；
  - 不中断主流程；
  - 下个周期继续采集。

#### 2.4 SNAT 统计边界（必须明确）

**接口设计决策**：`SNATStatsProvider` 应作为**独立接口**定义（如 `pkg/snat/stats.go`），**不修改现有 `snat.Manager` 接口**。原因：
- 当前 `snat.Manager` 是一个干净的声明式 reconcile 接口（`Reconcile` / `ReconcileForward` / `Cleanup`），加入 `Stats()` 会混入查询职责。
- Go 的隐式接口天然支持 `linuxManager` 同时实现 `Manager` 和 `StatsProvider`，而 `FakeManager` 不需要被迫实现 `Stats()`。
- Traffic collector 通过类型断言（`if sp, ok := snatMgr.(snat.StatsProvider); ok`）判断是否支持统计采集。

```go
// pkg/snat/stats.go — 独立接口，不修改 Manager
type StatsProvider interface {
    Stats() (map[string]SNATRuleStats, error)
}
```

- Linux 实现（`linuxManager`）解析 `iptables -t nat -vnL EZLB-SNAT`（或 nft 兼容路径）实现 `StatsProvider`。
- Fake 实现不实现 `StatsProvider`，collector 通过类型断言跳过 SNAT 统计采集。
- **第一阶段 NAT 统计按规则/后端维度输出，不承诺 service 粒度精确归因。**
- 原因：当前 SNAT rule key 仅由 `backendIP/backendPort/protocol` 组成；若多个 service 共享 backend，iptables 计数无法天然拆分到 service。
- 因此日志文案和 README 中需明确：
  - NAT 统计适合作为 backend / rule 层面的可观测性补充；
  - 不应被解读为跨共享 backend 场景下的精确 service 统计。

#### 2.5 Server 集成（`pkg/server/server.go`）

**运行模式区分**：
- **daemon 模式（`Run()`）**：启动 traffic collector goroutine。
- **once 模式（`RunOnce()`）**：执行一次 reconcile 后立即退出，**不启动 traffic collector**。

**注入依赖**：
- traffic logger
- nat logger
- stats provider（`LVSStatsProvider` + 可选 `SNATStatsProvider`）
- 当前配置快照

**配置热更新路径**：
当前 `config.Manager.OnChange()` 返回 `chan struct{}`，不携带变更内容。推荐采用**主动推送模式**（与现有 `healthMgr.UpdateTargets()` 模式一致）：
- Server 在 `OnChange` 回调中读取新配置快照，主动调用 collector 的更新方法（如 `collector.UpdateConfig(newCfg)`）。
- Collector 不应自行持有 config 引用或自行监听变更。
- 动态刷新的配置项：
  - `global.log.traffic.enabled`
  - `global.log.traffic.interval`
  - `global.log.traffic.include_snat`
  - `service.traffic_log`

**Shutdown 行为**：
- `shutdown()` 时停止 collector goroutine。
- 三套 logger 的 `Sync()` 必须在 `shutdown()` 中调用，确保日志刷盘后再退出。建议在 `server.shutdown()` 末尾统一调用，或由 `main.go` 通过 `defer logger.Sync()` 保证。

#### 2.6 日志输出字段

Traffic 日志（debug 级别，原始累计数据）包含：
- `service`
- `listen`
- `protocol`
- `scheduler`
- `source`（`ipvs`）
- `type`（`service` / `backend`）
- `backend_key`（仅 backend 类型）
- `connections`
- `bytes_in`
- `bytes_out`
- `packets_in`
- `packets_out`

NAT 日志（debug 级别，原始累计数据）包含：
- `source`（`snat`）
- `rule_key`
- `packets`
- `bytes`
- `source=snat`

### 3. 文档与测试产物

#### 3.1 文档更新
- 更新 `README` / `README_CN`：
  - 新增 `global.log` 配置块说明（含 `traffic` 子配置）
  - 新增 `traffic_log` 说明
  - 明确日志文件名与 home 默认值行为
  - 明确 NAT 统计归因边界
- 更新 `examples/ezlb.yaml`：
  - 增加日志配置示例（含 `global.log.traffic`）
  - 增加 service 级 `traffic_log` 示例
- 如存在 CHANGELOG / Release Note，记录此次可观测性增强与配置迁移。

#### 3.2 测试计划
- 单测：
  - 配置解析与默认值
  - logger 构建与 fallback
  - traffic collector 增量计算
  - `traffic_log=false/nil` 跳过
  - SNAT stats 解析
  - 计数清零 / rule 重建处理
- integration / e2e：
  - Linux 环境下验证日志文件生成
  - 短间隔 traffic logging 行为
  - 关闭 / `none` 后日志不再增长
  - 共享 backend 场景下 NAT 统计边界验证（至少验证日志表述不误导）

## 验证计划
1. 运行 `go test ./pkg/...`，覆盖配置解析、logger 构建、字段辅助、traffic logger、SNAT 解析等单元测试。
2. 针对 Linux 专属逻辑使用 build tag，确保 macOS/Linux CI 均可通过。
3. 运行新增的 integration / e2e 测试，验证 system / traffic / nat 日志文件行为、service 级别控制以及共享 backend 场景下的 NAT 统计边界。
4. 手工冒烟：`sudo ezlb start -c config.yaml`，检查 `${global.log.home}` 下的 `ezlb.log` / `traffic.log` / `nat.log`，并通过修改配置验证日志级别、日志目录、流量日志开关。

## 最终结论

该方案可以推进，但必须以"**日志基础设施先行、流量统计增强随后落地**"为原则实施。

编码前应以本修订版为准，特别注意以下约束：
1. `global.log` 是配置模型升级，`traffic` 配置内聚到 `global.log.traffic` 下；
2. `global.log.home` 默认值为 `./logs`；
3. Logger 初始化需处理时序问题：bootstrap logger → 加载配置 → 正式 logger；
4. 修复现有 `log_level` 未生效的 bug（`main.go` 中硬编码 InfoLevel）；
5. `LVSStatsProvider` 基于 `lvs.Manager` 适配器实现，不修改 `IPVSHandle` 接口；
6. `SNATStatsProvider` 作为独立接口，不修改 `snat.Manager` 接口；
7. NAT 统计第一阶段按规则/后端维度输出，不承诺 service 粒度精确归因；
8. Traffic collector 仅在 daemon 模式启动，once 模式不启动；
9. 新增 `gopkg.in/natefinch/lumberjack.v2` 依赖；
10. 必须先定义 stats provider 接口与快照模型，再进入实现。

---

## 评审修订记录

### Rev 1（2026-03-17）— 架构评审

基于项目代码实际架构（`pkg/config`、`pkg/server`、`pkg/lvs`、`pkg/snat`、`cmd/ezlb`）进行评审，主要修订：

| # | 修订项 | 说明 |
|---|--------|------|
| 1 | **配置结构内聚** | `traffic_logging` 从 `global` 平级收纳到 `global.log.traffic` 下 |
| 2 | **Logger 初始化时序** | 补充 bootstrap logger → config 加载 → 正式 logger 的初始化流程 |
| 3 | **修复现有 bug** | `main.go` 中 `newLogger()` 硬编码 InfoLevel，从未读取配置中的 log_level，纳入本次改造 |
| 4 | **LVSStatsProvider 实现策略** | 明确基于 `lvs.Manager` 适配器实现，不修改 `IPVSHandle` 接口 |
| 5 | **SNAT 接口设计** | `SNATStatsProvider` 作为独立接口，不修改 `snat.Manager`；通过类型断言判断支持 |
| 6 | **包路径简化** | `pkg/telemetry/trafficlogger/` → `pkg/trafficlog/`，保持扁平结构 |
| 7 | **热更新路径** | 明确采用主动推送模式（与 `healthMgr.UpdateTargets` 一致），collector 不自行监听 |
| 8 | **once 模式行为** | 明确 `RunOnce()` 不启动 traffic collector |
| 9 | **Shutdown Sync** | 补充三套 logger 的 `Sync()` 调用时机 |
| 10 | **CLI 参数纠正** | 删除"CLI 删除 `--log-level`"描述（当前代码中不存在此 CLI 参数） |
| 11 | **新依赖声明** | 明确需新增 `gopkg.in/natefinch/lumberjack.v2` |
| 12 | **类型精度** | 补充 `SvcStats.Connections`（uint32）→ `ServiceTrafficStats.Connections`（uint64）转换说明 |
| 13 | **Fake 环境行为** | 补充 macOS 开发环境下 collector 正常运行但不输出日志（delta 为 0）的预期行为 |

### Rev 3（后续版本）— 移除 NAT 日志记录功能

NAT 日志记录功能（`nat.log`、`include_snat`、`SNATStatsProvider`、`SNATRuleStats`）已于后续版本中移除以简化实现。具体变更：

- 删除 `Loggers.NAT` 字段及 `BuildLoggers` 中 NAT logger 构建逻辑，`nat.log` 文件不再生成
- 删除 `TrafficLogConfig.IncludeSNAT` 字段及 `IsIncludeSNAT()` 方法，`include_snat` 配置项不再支持
- 删除 `SNATRuleStats`、`TrafficSnapshot.SNAT`、`SNATStatsProvider` 接口
- 删除 `Collector` 中的 `natLogger`、`snatStats` 字段及相关 SNAT 统计采集逻辑
- SNAT 规则创建/删除/清理等操作日志改由 system logger（`logger.Named("snat")`）记录，输出到 stdout 和 `ezlb.log`
- `server.NewServer` 和 `newServerWithManager` 函数签名中删除 `natLogger` 参数

---

### Rev 2（2026-03-17）— Phase 2 流量日志行为调整

基于实际使用需求，对 Phase 2 流量日志行为进行以下调整：

| # | 修订项 | 说明 |
|---|--------|------|
| 1 | **记录原始数据** | 从增量 delta 模式改为记录原始累计数据（raw stats）。每次采集周期直接输出 IPVS 返回的累计统计值，不再维护 `prevSnapshot`、不再计算增量。移除 `safeDelta64` 和 `logAtLevel` 函数 |
| 2 | **默认不记录** | `ServiceConfig.traffic_log` 为 nil 时禁用。只有在 service 层面明确设置 `traffic_log: true` 时才记录 |
| 3 | **统一 debug 级别** | 所有 traffic / NAT 日志统一使用 debug 级别输出。Traffic / NAT logger 的文件 level 固定为 `zapcore.DebugLevel`，确保 debug 日志能写入文件 |
| 4 | **全局轮转规则** | Traffic / NAT 日志文件统一使用全局的 `LogConfig` 轮转规则（`max_size`、`max_backups`、`max_age`、`compress`），通过 `newLumberjackWriter` 复用 |
| 5 | **日志字段调整** | 字段从 `*_delta` 改为原始值：`connections`、`bytes_in`、`bytes_out`、`packets_in`、`packets_out`；SNAT 改为 `packets`、`bytes`。消息从 `"traffic stats"` / `"snat stats"` 改为 `"traffic raw stats"` / `"snat raw stats"` |
| 6 | **简化 Collector** | 移除 `prevSnapshot` 字段、`computeAndLogDeltas` 方法、`safeDelta64` 函数、`logAtLevel` 函数。新增 `logRawStats` 方法和 `isTrafficLogEnabled` 函数 |
